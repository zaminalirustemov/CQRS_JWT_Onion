@model List<CategoryListModel>

@{
    if (Model?.Count > 0)
    {
        <table class="table">
            <thead>
                <tr>
                    <td>ID</td>
                    <td>Definition</td>
                    <td>Actions</td>
                </tr>
            </thead>
            <tbody>
                @foreach (CategoryListModel categoryListModel in Model)
                {
                    <tr>
                        <td>@categoryListModel.Id</td>
                        <td>@categoryListModel.Definition</td>
                        <td>
                            <a asp-action="Remove" asp-controller="Category" asp-route-id="@categoryListModel.Id" class="btn btn-danger btn-sm">Sil</a>
                        </td>
                    </tr>
                }
            </tbody>

        </table>
    }
    else
    {
        <div class="text-center">
            Her hansi data movcud deyil
        </div>
    }
}